const BASE_LOCATION = './backend_dist';

export default class LocationConstants {
  public static DESCRIBE_INSTANCES_LAMBDA_SRC = `${BASE_LOCATION}/describe_instances_lambda`;
  public static START_INSTANCE_LAMBDA_SRC = `${BASE_LOCATION}/start_instance_lambda`;
}